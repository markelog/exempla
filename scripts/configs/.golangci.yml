enable:

run:
  deadline: 10m

linters:
  disable-all: true
  enable:
    - gofmt
    - gosimple
    - govet
    - ineffassign
    - typecheck
    - unconvert
    - unused
    - goconst
    - staticcheck

linter-settings:
  goconst:
    ignore-tests: true
